# Thư mục này lưu file camera_params.npz
# 
# Hướng dẫn:
# 1. Chạy Calibration-ZhangZhengyou-Method/calibrate.py với ảnh bàn cờ
# 2. Copy file camera_params.npz từ thư mục Calibration vào đây
#    Hoặc chạy: python copy_calib.py
#
# Nếu không có file này, AutoPano vẫn hoạt động bình thường
# (pipeline bỏ qua bước undistort một cách tự động)
